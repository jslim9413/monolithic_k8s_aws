# 메트릭 서버 설치
# kubectl apply -f https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml
# kubectl delete -f https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml

apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: orders-backend-hpa
  namespace: lgcns
spec: 
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: order-deployment
  
  # pod version 
  # minReplicas: 2
  # maxReplicas: 4
  # targetCPUUtilizationPercentage: 50
  
  # ec2 version
  minReplicas: 2
  maxReplicas: 10
  targetCPUUtilizationPercentage: 30

# 메트릭 서버와 HPA를 통한 pod 현황 조회
# kubectl get hpa orders-backend-hpa -n lgcns -w

# 부하 간단 테스트
# 특정 pod에 접속
# kubectl get pods -n lgcns
# kubectl exec -it ordersystem-backend-69dd989f9b-9d4ks -n lgcns -- sh

# curl install 
# apk add --no-cache curl
# while true; do curl -s http://order-service/product/list; done

